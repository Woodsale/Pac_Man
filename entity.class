public class entity{
  int x;
  int y;
  int length;
  int height;
  String direction; #there must be a better data type for this, maybe make a class for cardinality?
  int speed; #pixels every tick

  public entity(){
    x = 0;
    y = 0;
    length = 25
    height = 25
    direction = "up";
    speed = 1;
  }
  
  #to do: add getters and setters for all current attributes


}
